import SwiftUI

struct ListView: View {
    var body: some View {
        Text("List")
    }
}

struct ListView_Previews: PreviewProvider {
    static var previews: some View {
        ListView()
    }
}
